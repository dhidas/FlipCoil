#include "PVariables.pmc"
#include "MVariables.pmc"

Open PROG 5 Clear

LINEAR
ABS
TA300
TS100

FRAX(A,B)
F(P_RotarySpeed_DegPerS)

; Check if motors homed or not
if (M_HomeComplete_M5 != 1 or M_HomeComplete_M6 != 1)
  return
endif


Close
