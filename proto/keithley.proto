OutTerminator = CR LF;
InTerminator = CR LF;

get_fields {
  Separator = ',';
  extrainput = ignore;
  replyTimeout = 1000;
  out "FETCH?";
  in "%f";
}
get_tracedata {
  Separator = ',';
  extrainput = ignore;
  replyTimeout = 1000;
  out ":TRAC:DATA?";
  in "%f";
}


sendtxt {
  out "%s";
}

